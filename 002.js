const first = ['Behind', 'every', 'great', 'man']
const second = ['is', 'a', 'woman']
const third = ['rolling', 'her', 'eyes']

var join = first.concat(second, third);

var sentence = join.join(' ');

console.log(sentence);